#include <stdio.h>

int main(){
    int numerik;
    char ASCII;
    scanf("%d", &numerik);
    scanf("%c", &ASCII);

    if (numerik >= 0 && numerik <= 200);
    printf("%d", &numerik);
    printf("%c", &ASCII);

    return 0;
}